CREATE TABLE company(
                        name varchar(10),
                        address VARCHAR(255)
);
CREATE TABLE user(
                     username varchar(10),
                     address VARCHAR(255)
);
